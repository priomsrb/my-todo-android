<?xml version="1.0" encoding="utf-8"?>
<!--
  What the widget picker shows for the "one list" widget. Static by necessity: a preview is drawn
  before any widget exists, so there is no list to read. Kept in step with ListWidget by hand.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_preview_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="12dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingBottom="6dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/widget_preview_list_name"
                android:textColor="@color/widget_preview_text"
                android:textSize="15sp"
                android:textStyle="bold" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_preview_count"
                android:textColor="@color/widget_preview_text_secondary"
                android:textSize="12sp" />
        </LinearLayout>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingVertical="4dp"
            android:text="@string/widget_preview_item_one"
            android:textColor="@color/widget_preview_text"
            android:textSize="14sp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="12dp"
            android:paddingEnd="0dp"
            android:paddingVertical="4dp"
            android:text="@string/widget_preview_item_two"
            android:textColor="@color/widget_preview_text"
            android:textSize="14sp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingVertical="4dp"
            android:text="@string/widget_preview_item_done"
            android:textColor="@color/widget_preview_text_secondary"
            android:textSize="14sp" />
    </LinearLayout>

    <!-- The two action buttons, floating in the corner exactly as ListWidget draws them. -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="12dp"
        android:orientation="horizontal">

        <ImageView
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="@drawable/widget_voice_button"
            android:contentDescription="@string/voice_widget_label"
            android:padding="8dp"
            android:src="@drawable/ic_mic"
            android:tint="@color/widget_preview_on_accent" />

        <ImageView
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_marginStart="8dp"
            android:background="@drawable/widget_voice_button"
            android:contentDescription="@string/new_item"
            android:padding="8dp"
            android:src="@drawable/ic_add"
            android:tint="@color/widget_preview_on_accent" />
    </LinearLayout>
</FrameLayout>
